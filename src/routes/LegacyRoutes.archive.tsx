/**
 * Legacy platform routes — disabled. Preserved for reference only.
 * Re-enable by copying routes back into App.tsx.
 *
 * import Index from "./pages/Index";
 * import Jobs from "./pages/Jobs";
 * ... (see git history for full import list)
 *
 * <Route path="/" element={<Index />} />
 * <Route path="/legacy" element={<Index />} />
 * <Route path="/auth" element={<Auth />} />
 * <Route path="/jobs" element={<Jobs />} />
 * ... employer, admin, legacy worker (Supabase), etc.
 */

export {};
